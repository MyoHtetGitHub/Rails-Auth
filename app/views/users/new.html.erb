<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title></title>
	<link rel="stylesheet" href="">
</head>
<body>
	<%= render 'layouts/navbar' %>
	<div class="container">
		<div class="row">
			<div class="col-sm-6 offset-3">
			<div class="card">
				<div class="card card-header">
					<h5 class="text-center text-danger">Sign Up </h5>
				</div>
				<div class="card card-body">
				<%= form_for(@user, :url => {:controller => 'users', :action => 'create'}) do |f| %>
					<div class="form-group">
						<p> Username:</br> <%= f.text_field :name, class: 'form-control' %> </p>
					</div>
					<div class="form-group">
						<p> Email:</br> <%= f.text_field :email, class: 'form-control' %> </p>
					</div>
					<div class="form-group">
						<p> Password:</br> <%= f.password_field :password,  class: 'form-control' %> </p>
					</div>
					<div class="form-group">
						<p> Password Confirm:</br> <%= f.password_field :password_confirmation , class: 'form-control' %> </p>
					</div>
					<div class="form-group">
					 <%= f.submit :Signup, class: 'btn btn-success' %>
					</div>
					<% end %>
				</div>
			</div>
			</div>
		</div>
	</div>
</body>
</html>